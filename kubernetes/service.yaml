apiVersion: v1
kind: Service
metadata:
  name: my-service
  nnamespace: reddy
spec:
  selector:
    app: dotnetdemo
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9000